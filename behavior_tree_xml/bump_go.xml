<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
    <BehaviorTree ID="bump_go">
        <Sequence>
            <Fallback>
                <IsClear/>
                <Turn/>
            </Fallback>
            <GoStraight/>
        </Sequence>
    </BehaviorTree>

    <!-- Description of Node Models (used by Groot) -->
    <TreeNodesModel>
        <Action ID="GoStraight"
                editable="true"/>
        <Condition ID="IsClear"
                   editable="true"/>
        <Action ID="Turn"
                editable="true"/>
    </TreeNodesModel>

</root>
